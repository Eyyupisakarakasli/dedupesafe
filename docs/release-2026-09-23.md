# Production release — 23 September 2026

The user explicitly authorized committing, pushing and deploying the previously local design and review changes.

## Verified application revision

- Source: `516d69eee464b4521ade370124367db6c6e883c4` on `master`.
- GitHub CI: https://github.com/Eyyupisakarakasli/dedupesafe/actions/runs/35865621752 — completed successfully.
- Vercel production deployment: `dpl_Gh8YhJko7ST3RwLVaLXRHxD5p3p9`, READY.
- Immutable deployment: https://dedupesafe-jdb7ap451-eyyupisakarakaslis-projects.vercel.app
- Production alias verified: https://dedupesafe.vercel.app
- GitHub Vercel status for the source revision points to this deployment and reports success.

Local validation: 94 unit tests, 8 Chromium browser tests, lint and build passed. Dependency audit found zero vulnerabilities. All 8 browser tests were then repeated successfully against the production alias, covering upload, worker matching, row selection, exclusions, audit and reviewed CSV downloads, focus, pagination and authorization resets.

The production checker returns HTTP 200, contains its restrictive `connect-src 'none'` CSP and has no analytics script. Public production JS is `/assets/app-BLjPiX4V.js`; CSS is `/assets/app-DGyW8mCW.css`. Local and remote JS filenames differ, so filename equality was not used as deployment proof; GitHub deployment status and the live behavior tests provide verification.

The README and this release record follow the tested application revision as documentation-only commits. Subsequent deployments should be verified against their own GitHub revision and CI run. Earlier local-only statements in design/audit notes describe the pre-release phase, not current deployment authorization or status.
