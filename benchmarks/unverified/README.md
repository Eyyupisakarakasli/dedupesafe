# Unverified inherited benchmark

`pre-commit-2026-09-22.json` preserves the pre-existing working-tree change found
before committing pending work on 22 September 2026. Its timestamp predates the
previous committed latest report, and its durations differ substantially.
The original run and its conditions could not be verified. Do not use this file
for performance claims or treat it as the current benchmark. It is retained so
that refreshing `../latest.json` does not erase the inherited evidence.
